import { ComingSoonPage } from "@/components/pages/coming-soon";

export default function PricingPage() {
  return (
    <ComingSoonPage
      title="단가 프리셋"
      description="고정금액, 수량 × 단가, 초당 단가, 비율 등 개인별 계산 규칙을 저장하는 화면입니다."
      nextVersion="v0.3.0"
    />
  );
}
